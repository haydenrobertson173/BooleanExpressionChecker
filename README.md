# BooleanExpressionChecker
A terminal-based Java program that validates / evaluates boolean expressions. It prompts the user for the number of inputs and the boolean expression.  It then generates all possible input combinations (or accepts user inputs), and outputs whether each combination produces true or false.
